{
  "schema": "d7460n-progress/1",
  "read_order": [
    "PROGRESS.json",
    "newest shard in shards[]"
  ],
  "meta": {
    "goal": "Build github.com/Autocss-com/autocss = the refined, completed version of `starter`. Measuring stick: the finished app must look, behave, and work like `dhcp` (the working prototype), changing nothing except to improve to the D7460N Architecture. Separate repo so the two originals are never touched.",
    "repos": {
      "autocss": "this repo - best-of-both: starter's architecture, completed using dhcp as the target, every feature rewritten to D7460N.",
      "starter": "the D7460N-correct rewrite of dhcp; CSS-driven visibility done, but incomplete. Architecture reference.",
      "dhcp": "messy-but-complete working prototype; the FEATURE + LOOK reference (an MVP, a floor not a ceiling)."
    },
    "rules": [
      "CLAUDE.md in this repo is canonical and non-negotiable.",
      "HTML+CSS do everything; JS is CRUD-only (API transport).",
      "JS's single UI bridge is `oninput` on state-machine <input>s - fired by BOTH user action AND programmatically (initial load/restore = select the nav radio: set .checked=true + input.dispatchEvent(new Event('input',{bubbles:true})) so the input's OWN oninput fires). NEVER click/onclick/change/addEventListener/dynamic import(). dispatchEvent is the SINGLE SANCTIONED exception: used ONLY to make a programmatically-selected nav radio fire its own oninput on load/restore (no real user event exists there); all real user interactions fire oninput natively - no dispatch.",
      "No data-*/class/id; drive dirty/valid/enabled via CSS :has(:valid)/:has(:invalid)/:checked/:empty/:not(:empty)/aria-disabled.",
      "JSON -> elements via toTagName() is allowed; no innerHTML string-building for presentation.",
      "No utils/helpers/utility file names - name each module for what it DOES.",
      "Semantic clarity beats cleverness; modern ES6+ baseline; stateless/idempotent; one API base.",
      "Size: smaller is a guideline not a rule; D7460N compliance may need more code - if autocss ends up larger than dhcp, explain why."
    ],
    "heuristic": "Per feature (HTML + companion CSS + companion JS together, companion tracking mandatory): (1) what does it do; (2) is there a MORE MODERN native HTML/CSS way - if yes, STOP and ASK the user before changing markup/CSS, every time (identifying != applying); if no, keep unchanged; (3) will the universal 'one UI to rule them all' app ever need it - YES is the overwhelming default = KEEP & MODERNIZE. Dropping is a rare exception: only if truly redundant, and ALWAYS confirm with user first. Partial/half-built features: inspect, default to COMPLETE not drop.",
    "verify": "Enabled env: network=Full (data hosts + Playwright CDN), browser=Playwright chromium via setup script. Tests = JS-logic node unit tests + static/compliance checks + Playwright DOM/behavioral/regression. Pixel-perfect bleeding-edge CSS fidelity needs a HUMAN eye (user does this manually).",
    "cadence": "File-by-file, feature-by-feature. Stop and ask on ANY doubt. Check in before FIRST push and after each numbered build step. Test each feature + run regression as we go. Keep companions in lockstep."
  },
  "build_order": [
    "1. Repo skeleton + PROGRESS.json + SessionStart hook + CLAUDE.md.",
    "2. CSS base (starter set; reconcile dhcp concerns; inspect candidate-drops diag/stars/hero/_* and confirm with user before dropping).",
    "3. index.html single universal scaffold (incl. dropdown-capable nav + its CSS).",
    "4. Transport+lifecycle: config/env/api(+write methods)/storage/oninput.",
    "5. Data shaping: schema(dhcp domain for now)/rules/ + concern-named modules (NEVER utils/helpers) - node tests.",
    "6. Generation inject (toTagName, no innerHTML/events).",
    "7. CRUD UI forms + forms.css(de-data-*) + fallbacks.css + loading.css.",
    "8. Wire dhcp datasets; full behavioral + regression pass in the enabled env."
  ],
  "_self": {
    "append_to": "newest shard in shards[]",
    "rotate_at_lines": 1500,
    "on_rotate": "start progress/log-NNN.ndjson (next number), register it in shards[], update cursor.",
    "never": "never edit past records; only append corrections (record type=lesson or note).",
    "resume": "read PROGRESS.json first, then the newest shard, to restore context + why.",
    "record_types": [
      "decision",
      "feature",
      "test",
      "note",
      "setup",
      "lesson"
    ],
    "feature_record_shape": {
      "name": "",
      "html": "",
      "css": [],
      "js": [],
      "verdict": "keep|merge|drop|complete",
      "status": "todo|in-progress|done",
      "why": "",
      "tested": false
    }
  },
  "shards": [
    "progress/log-001.ndjson"
  ],
  "cursor": {
    "phase": "Steps 1-7 DONE+verified. Step 7 = DHCP CRUD ported AS-IS to parity (forms.js+utils.js+inject form fns+two-input row-toggle+oninput getFieldRules+forms/fallbacks/loading.css), verified on `manage` via REAL triggers in chromium-over-http. NEXT = step 8 (all 12 datasets + regression = full parity), THEN refine compliance + add PWA in SEPARATE future sessions. comply-later = THIS BUILD ONLY.",
    "last": "Built step 7 on claude/lucid-hawking-E5Ej2 (container had wrongly checked out empty claude/great-ride-37t354; switched; user confirmed lucid-hawking + throwaway-record Save/Delete test). Ported DHCP forms.js/utils.js as-is, added inject form fns + two-input row-toggle (radio.dispatchEvent as-is) + getFieldRules cache + 3 CSS files. Verified real-trigger: read/row-select/live-mirror/Reset/New/Close/Save(POST live, cleaned up)/Delete(DOM-only per id bug). 27/27 chromium checks + 7/7 node tests. As-is bugs (POST-always/POST-top-level, Delete DOM-only, label>id) ported + logged in LEDGER. Committed + pushed.",
    "next": "Step 8 = exercise ALL 12 nav endpoints (manage, faqs, api-registration, audit, option-set, option-types, scope-type, server-types, servers, credentials, variables, settings): confirm each loads + its form renders like DHCP; full behavioral + regression pass (node + chromium). Completes DHCP PARITY. Then FUTURE SESSIONS backlog (PWA; forms-compliance ledger; unsaved-guard feature; state-persistence; color-scheme), each its own session + handoff.",
    "open_q": [
      "PORT-AS-IS is the active strategy (comply-later = THIS BUILD ONLY). Compliance fixes + PWA are SEPARATE future sessions - see SESSION-HANDOFF COMPLIANCE-DEBT LEDGER + FUTURE SESSIONS.",
      "id-lookup behavior (keep faithful POST-only vs fix to PUT/DELETE by id- cell) - forms-compliance session.",
      "error display (DHCP intro p vs form aria-live p) - compliance session.",
      "loading spinner (DHCP dual-ring vs autocss single) - compliance session.",
      "unsaved-changes guard = NEW feature (Popover API); own session; DHCP has none; do NOT add during port.",
      "state-persistence generalization (decision D); own session; auto-restore is NAV-ONLY for now.",
      "PWA manifest.webmanifest + sw.js - own session.",
      "color-scheme dark (DHCP) vs Rule 19 light-dark - compliance/cross-cutting.",
      "confirm API base = DHCP mockapi (effectively confirmed; steps 4-6 work)."
    ]
  }
}
